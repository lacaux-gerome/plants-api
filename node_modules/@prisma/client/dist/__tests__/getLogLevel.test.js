"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const getLogLevel_1 = require("../utils/getLogLevel");
test('info and warn', () => {
    const level = getLogLevel_1.getLogLevel([
        {
            emit: 'event',
            level: 'info',
        },
        {
            emit: 'event',
            level: 'warn',
        },
    ]);
    expect(level).toMatchInlineSnapshot(`"info"`);
});
test('query', () => {
    const level = getLogLevel_1.getLogLevel([
        {
            emit: 'event',
            level: 'query',
        },
    ]);
    expect(level).toMatchInlineSnapshot(`undefined`);
});
test('strings and objects', () => {
    const level = getLogLevel_1.getLogLevel([
        {
            emit: 'event',
            level: 'query',
        },
        'warn',
    ]);
    expect(level).toMatchInlineSnapshot(`"warn"`);
});
//# sourceMappingURL=getLogLevel.test.js.map