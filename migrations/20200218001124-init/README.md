# Migration `20200218001124-init`

This migration has been generated by Gérome Lacaux at 2/18/2020, 12:11:24 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00',
    "email" text  NOT NULL DEFAULT '',
    "firstName" text   ,
    "id" text  NOT NULL ,
    "password" text  NOT NULL DEFAULT '',
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."PlantBox" (
    "description" text  NOT NULL DEFAULT '',
    "id" SERIAL,
    "image" text  NOT NULL DEFAULT '',
    "name" text  NOT NULL DEFAULT '',
    "user" text   ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Plant" (
    "description" text  NOT NULL DEFAULT '',
    "id" SERIAL,
    "image" text   ,
    "name" text  NOT NULL DEFAULT '',
    "plantBox" integer   ,
    "soilTypes" text  NOT NULL DEFAULT 'SANDY',
    "sprayFrequency" integer  NOT NULL DEFAULT 0,
    "sunExposure" text  NOT NULL DEFAULT 'FULL_SUN',
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

ALTER TABLE "public"."PlantBox" ADD FOREIGN KEY ("user") REFERENCES "public"."User"("id") ON DELETE SET NULL

ALTER TABLE "public"."Plant" ADD FOREIGN KEY ("plantBox") REFERENCES "public"."PlantBox"("id") ON DELETE SET NULL
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200215014508-init..20200218001124-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://admin:admin_psw@localhost:5432/plants"
 }
 generator client {
   provider = "prisma-client-js"
```


