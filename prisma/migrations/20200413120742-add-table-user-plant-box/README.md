# Migration `20200413120742-add-table-user-plant-box`

This migration has been generated by Gérome Lacaux at 4/13/2020, 12:07:42 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "email" text  NOT NULL ,
    "firstName" text   ,
    "id" text  NOT NULL ,
    "lastName" text   ,
    "password" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."PlantBox" (
    "description" text  NOT NULL ,
    "id" SERIAL,
    "image" text  NOT NULL ,
    "name" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

ALTER TABLE "public"."Plant" ADD COLUMN "plantBoxId" integer   ;

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

ALTER TABLE "public"."Plant" ADD FOREIGN KEY ("plantBoxId")REFERENCES "public"."PlantBox"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200413114823-initial..20200413120742-add-table-user-plant-box
--- datamodel.dml
+++ datamodel.dml
@@ -1,21 +1,40 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://admin:admin_psw@localhost:5432/plants"
 }
 generator client {
   provider = "prisma-client-js"
 }
+model User {
+  id        String   @default(cuid()) @id
+  createdAt DateTime @default(now())
+  password  String
+  email     String   @unique
+  firstName String?
+  lastName  String?
+}
+
+model PlantBox {
+  id          Int     @default(autoincrement()) @id
+  name        String
+  description String
+  image       String
+  plants      Plant[]
+}
+
 model Plant {
   id             Int         @default(autoincrement()) @id
   name           String
   description    String
   sprayFrequency Int
   sunExposure    SunExposure
   image          String?
   soilTypes      Soil
+  plantBox       PlantBox?   @relation(fields: [plantBoxId], references: [id])
+  plantBoxId     Int?
 }
 enum Soil {
   SANDY
```


