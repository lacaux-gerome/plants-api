# Migration `20200412144123-initial-tables`

This migration has been generated by Gérome Lacaux at 4/12/2020, 2:41:23 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "Soil" AS ENUM ('SANDY', 'CLAY', 'SILT');

CREATE TYPE "SunExposure" AS ENUM ('FULL_SUN', 'HALF_SUN', 'NO_SUN');

CREATE TABLE "public"."User" (
    "createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "email" text  NOT NULL ,
    "firstName" text   ,
    "id" text  NOT NULL ,
    "password" text  NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."PlantBox" (
    "description" text  NOT NULL ,
    "id" SERIAL,
    "image" text  NOT NULL ,
    "name" text  NOT NULL ,
    "userId" text   ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "public"."Plant" (
    "description" text  NOT NULL ,
    "id" SERIAL,
    "image" text   ,
    "name" text  NOT NULL ,
    "plantBoxId" integer   ,
    "soilTypes" "Soil" NOT NULL ,
    "sprayFrequency" integer  NOT NULL ,
    "sunExposure" "SunExposure" NOT NULL ,
    PRIMARY KEY ("id")
) 

CREATE UNIQUE INDEX "User.email" ON "public"."User"("email")

ALTER TABLE "public"."PlantBox" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE SET NULL  ON UPDATE CASCADE

ALTER TABLE "public"."Plant" ADD FOREIGN KEY ("plantBoxId")REFERENCES "public"."PlantBox"("id") ON DELETE SET NULL  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200412144123-initial-tables
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,51 @@
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://admin:admin_psw@localhost:5432/plants"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id        String     @default(cuid()) @id
+  createdAt DateTime   @default(now())
+  email     String     @unique
+  firstName String?
+  password  String
+  box       PlantBox[]
+}
+
+model PlantBox {
+  id          Int     @default(autoincrement()) @id
+  name        String
+  description String
+  image       String
+  plant       Plant[]
+  owner       User?   @relation(fields: [userId], references: [id])
+  userId      String?
+}
+
+model Plant {
+  id             Int         @default(autoincrement()) @id
+  name           String
+  description    String
+  sprayFrequency Int
+  sunExposure    SunExposure
+  image          String?
+  soilTypes      Soil
+  box            PlantBox?   @relation(fields: [plantBoxId], references: [id])
+  plantBoxId     Int?
+}
+
+enum Soil {
+  SANDY
+  CLAY
+  SILT
+}
+
+enum SunExposure {
+  FULL_SUN
+  HALF_SUN
+  NO_SUN
+}
```


