# Migration `20200426194627-user-role`

This migration has been generated by Gérome Lacaux at 4/26/2020, 7:46:27 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TYPE "UserRole" AS ENUM ('ADMIN', 'USER');

CREATE TYPE "ShortExposure" AS ENUM ('FULL_SUN', 'HALF_SUN', 'NO_SUN');

CREATE TYPE "CardinalPoint" AS ENUM ('NORTH', 'SOUTH', 'EAST', 'WEST');

ALTER TABLE "public"."Plant" DROP COLUMN "sunExposure",
ADD COLUMN "cardinalPoint" "CardinalPoint"[]  ,
ADD COLUMN "shortExposure" "ShortExposure" NOT NULL ,
DROP COLUMN "soilTypes",
ADD COLUMN "soilTypes" "Soil"[]  ;

ALTER TABLE "public"."User" ADD COLUMN "role" "UserRole" NOT NULL DEFAULT 'USER';

DROP TYPE "SunExposure"
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200413120742-add-table-user-plant-box..20200426194627-user-role
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = "postgresql://admin:admin_psw@localhost:5432/plants"
 }
 generator client {
   provider = "prisma-client-js"
@@ -11,12 +11,18 @@
   id        String   @default(cuid()) @id
   createdAt DateTime @default(now())
   password  String
   email     String   @unique
+  role      UserRole @default(USER)
   firstName String?
   lastName  String?
 }
+enum UserRole {
+  ADMIN
+  USER
+}
+
 model PlantBox {
   id          Int     @default(autoincrement()) @id
   name        String
   description String
@@ -24,16 +30,17 @@
   plants      Plant[]
 }
 model Plant {
-  id             Int         @default(autoincrement()) @id
+  id             Int             @default(autoincrement()) @id
   name           String
   description    String
   sprayFrequency Int
-  sunExposure    SunExposure
+  cardinalPoint  CardinalPoint[]
+  shortExposure  ShortExposure
   image          String?
-  soilTypes      Soil
-  plantBox       PlantBox?   @relation(fields: [plantBoxId], references: [id])
+  soilTypes      Soil[]
+  plantBox       PlantBox?       @relation(fields: [plantBoxId], references: [id])
   plantBoxId     Int?
 }
 enum Soil {
@@ -41,9 +48,16 @@
   CLAY
   SILT
 }
-enum SunExposure {
+enum ShortExposure {
   FULL_SUN
   HALF_SUN
   NO_SUN
+}
+
+enum CardinalPoint {
+  NORTH
+  SOUTH
+  EAST
+  WEST
 }
```


